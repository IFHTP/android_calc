package com.example.android_calc.ui.theme

import androidx.compose.ui.graphics.Color

val OrangePrimary = Color(0xFFFF9500)   // Фирменный оранжевый (операторы, =)
val DeepBlack = Color(0xFF000000)       // Чистый черный (фон темной темы)
val DarkGraySurface = Color(0xFF222222) // Темно-серый (кнопки цифр в темной теме)
val LightGraySurface = Color(0xFFE0E0E0) // Светло-серый (кнопки цифр в светлой теме)
val ActionGray = Color(0xFFA5A5A5)      // Серый для спецкнопок (AC, Backspace)

val WhiteText = Color(0xFFFFFFFF)
val BlackText = Color(0xFF000000)

val OffWhiteBackground = Color(0xFFF5F5F5) // Фон экрана в светлой теме